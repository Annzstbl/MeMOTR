从 model_20260511 复制。

改动摘要：
- 新增 `FigureContext` + `figure_save.py`：推理时由模型内部直接保存可视化图，无需外部 hook。
- `MSDeformAttnFigure`：cross-attn forward 内原生捕获 `attention_weights` / `sampling_locations`。
- `DeformableDecoder` 汇总各层 cross-attn debug；`MeMOTR.forward(figure=...)` 在 forward 末尾触发保存。
- 配置：`MEMOTR_VERSION: '20260511_figure'`
- 用法见 `debug/20260526/test2.py`
