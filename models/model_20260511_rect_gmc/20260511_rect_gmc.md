# model_20260511_rect_gmc

从 `model_20260511_rect` 复制；**推理**时在 `get_reference_points` 之前用 GMC 更新已有 track 的 `ref_pts`（4D cxcywh，`compensate_rect_boxes`）。

- VT-Tiny / 正框训练权重须用本版本，不要用 `20260511_gmc`（旋转框 5D + 8 通道基座）。
- `RECT_MEMOTR_VERSIONS` 含 `20260511_rect_gmc`，submit 时 `bbox_dim=4`。

## Submit 配置（252）

| 实验 | yaml |
|------|------|
| 20260511-4 | `configs_hsmot_spectral_embed_252/20260511-4-rect-gmc-submit.yaml` |
| 20260511-3 | `configs_hsmot_spectral_embed_252/20260511-3-rect-gmc-submit.yaml` |

```bash
cd MeMOTR
PY=/path/to/hsmot310/bin/python
$PY scripts/batch_submit_eval_hsmot_gmc_ckpts.py \
  --config-path configs_hsmot_spectral_embed_252/20260511-4-rect-gmc-submit.yaml \
  --ckpts last.pth --miss-tolerance 5 --force \
  --available-gpus 0,1 --submit-threads 4 --last-epoch 18
```
